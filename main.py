# -*- coding: utf-8 -*-
"""
Year 2 Project
Adam Brzosko
Main
"""
import numpy as np
import pylab as pl

E = (Ball.mass*np.dot(Ball.vel))/2

   ''' def next_collision(self): 
        t = self._ball.time_to_collision(self._container)
        self._ball.move(t)
        self._ball.collide(self._container)'''